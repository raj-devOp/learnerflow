# Terraform state — may contain secrets, never commit
*.tfstate
*.tfstate.*

# Downloaded providers (large, machine-specific)
.terraform/

# Crash logs and variable files with secrets
crash.log
*.tfvars
